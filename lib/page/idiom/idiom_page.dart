import 'package:flutter/material.dart';

class IdiomPage extends StatelessWidget {
  const IdiomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(child: Text('idiom')),
    );
  }
}
